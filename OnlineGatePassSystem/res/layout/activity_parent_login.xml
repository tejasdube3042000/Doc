<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/user"
    tools:context=".ParentLoginActivity" >

    <Button
        android:id="@+id/pregister"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/plogin"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="88dp"
        android:background="@drawable/roundbutton"
        android:text="Register"
        android:textSize="25dp" />

    <Button
        android:id="@+id/plogin"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/pregister"
        android:layout_alignLeft="@+id/parentpassword"
        android:layout_marginBottom="34dp"
        android:background="@drawable/roundbutton"
        android:text="Login"
        android:textSize="25dp" />

    <EditText
        android:id="@+id/parentpassword"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_above="@+id/plogin"
        android:layout_alignLeft="@+id/parentname"
        android:layout_marginBottom="28dp"
        android:background="@drawable/roundedit"
        android:ems="10"
        android:hint="Enter YourPassword"
        android:inputType="textPassword"
        android:textColorHint="#000000" />

    <EditText
        android:id="@+id/parentname"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_above="@+id/parentpassword"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="24dp"
        android:background="@drawable/roundedit"
        android:ems="10"
        android:hint="Enter Your Name"
        android:inputType="textPersonName"
        android:textColorHint="#000000" />

</RelativeLayout>
